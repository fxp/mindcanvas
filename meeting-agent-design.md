# 实时会议 Agent · 设计与功能清单（Living Doc）

v0.3 · 2026-06-18 · 持续更新

> **一句话定位**：把"一人讲、几百人听"的单向广播，变成一块实时把群体声音收敛成连贯内容的协作认知画布。
> **真正的壁垒**：不是转录+摘要，而是 many-to-one 的实时综合——LLM 在中间把几百个声音收敛成一份连贯、可读、可参与的东西。

> v0.3 变更：新增**视觉通道**（定期拍照，对准主讲 + PPT）。幻灯片成为章节结构的真值来源；相应更新状态模型、锚定、聚类、路线图。

---

## 1. 已确认的产品决策

| 维度 | 决策 |
|---|---|
| 观众输入 | 纯文字 + 一键 emoji；**不开语音**（避免打扰主讲）。文件上传列入后续。 |
| 视觉输入 | Agent 定期拍照（**对准主讲 + PPT 的视角**），翻页时解析。若现场可取数字投屏/HDMI 信号则优先用于幻灯片文本，摄像头补主讲 / 指向 / 白板 / 实物。 |
| 匿名 | 允许完全匿名（可选会话级临时 token，见 §2.6）。 |
| 呈现 | 状态与渲染分离，支持多种**可切换 view**。 |
| 评论锚定 | 每条评论/emoji 带时间戳，默认自动对应到当时正在讲的内容节点 + 当时所在幻灯片；也可手动指定节点。 |
| 聚类 | 按"评论所针对的内容节点"做**局部聚类**，不做全局聚类。 |

---

## 2. 核心机制

### 2.1 结构状态 = 带时间轴的提纲树（三模态，一棵树）
状态是一棵提纲树（canonical state）。**幻灯片定章节骨架，语音填页内讲解，评论/emoji 挂节点。**
每个节点包含：要点文本、时间区间 `[t_start, t_end]`、**关联幻灯片**（slide id + 裁图 + OCR 文本）、挂载的评论簇 / emoji 计数 / 投票、元数据（热度、争议度、困惑度）。所有处理都是对这棵树打 patch，不重新生成全文。
好处：连贯性（永远 patch 现有结构）、可回放（event-sourced 天然支持时间旅行）、可扩展（贵的推理只在中心做一次，渲染结果向几百端廉价广播）。

### 2.2 视觉通道（主讲 + 幻灯片）
三条输入通道（语音 ASR / 文字+emoji / 视觉帧）都是 event，喂同一个 reducer。视觉通道：
- **幻灯片是结构真值**：翻页 = 最干净的话题边界；slide 标题 → section header（优于 ASR 派生）。
- **页内细分**：一页讲很久时，靠语音在页内继续分小节。
- **off-deck 优雅退回**：临场 demo / 白板 / Q&A 时退回语音驱动，不硬套页结构。
- **抓语音抓不到的**：OCR + VLM 取图表数字、表格、公式、代码；真实图表**裁剪后直接嵌入画布**；用 slide 文本反向纠正嘈杂 / 中英混说的 ASR。
- **指代消解（deixis）**：同步帧 + 指向检测，把"看这里 / this part"解析到主讲正指的区域。
- **效率**：别每帧 VLM。感知哈希 / 帧差检测翻页，**只在翻页时**跑一次 OCR/VLM（30 页 ≈ 30 次调用）。主讲区分析低频或事件触发（ASR 出现指代词时抓帧）。
- **部署**：帧处理放本地，只回传结构化 slide 对象 + 裁图，原始视频不出设备（边端协同 / 会场盒子形态）。摄像头对准舞台与屏幕，勿扫观众。

### 2.3 时间戳 → 内容 自动锚定
- 每条评论/emoji 带时间戳 `t`，默认锚定到 `t` 落入区间的节点 **+ 当时所在幻灯片** → 自动对应"当时在讲什么、在放哪页"。
- **关键细节**：时间戳取**开始撰写时**（点"评论"那一刻 / 当时屏幕锚点），不是发送时。否则一条写了 30 秒的评论，发出去时主讲已翻篇，就锚错。撰写时显示"你正在评论：XX"、可手动改。
- 也允许**显式指定**：点某个节点直接对它评论，无视时间。

### 2.4 上下文内聚类（系统脊柱）
评论只在其锚定节点（及相邻节点）范围内聚类，不做全局聚类。
1. 更便宜、更准：在语义一致的小集合里去重/聚类。
2. 信号更可操作："5 人在问**这个具体点**" 远胜全局"有人提问"。
3. **一次锚定，喂养半个功能清单**——每个信号都挂在节点 / 幻灯片上之后：
   - 困惑热力图 = 各节点 / 各页的 😕 / 提问 计数（**可按页算**："第 7 页让全场困惑了"）
   - 争议点检测 = 节点内评论的立场方差
   - 自动问题队列 = 按节点分组的问题簇
   - 观点光谱 = 节点内立场向量聚类
   全部从节点信号"免费"派生。
4. 粗细两级分段：**幻灯片边界 = 粗粒度章节**；语音 = 页内细粒度小节。
5. 二次 pass：把跨节点反复出现的同主题簇连起来（"这个问题在三处被问到"）。

### 2.5 emoji = 零成本信号
一键 emoji 走同一套锚定。😕 喂困惑热力图、🔥/👍 喂注意力与共识——纯计数、无需 NLP、匿名零门槛。

### 2.6 匿名与会话身份
完全匿名对人/对主持人都成立。可发一个**与真实身份无关、散会即弃的会话级随机 token**：既不暴露任何人，又能支撑"同一匿名者的立场演变"（观点光谱）与防刷票。是否开启可配置。

### 2.7 规模（几百人）四件套
1. 评论窗口批处理（攒 5–10 秒一批）。
2. 廉价前置过滤（embedding 去重/聚类 + spam/毒性）。
3. 分层模型（小模型 triage，大模型综合）。
4. surface 而非全显（只展示 Top-N 簇，其余折叠可搜）。
5. 帧：感知哈希只在翻页时跑 VLM（见 §2.2）。

---

## 3. 呈现：可切换的多 View
状态与渲染分离，加 view 几乎零成本（只是换 renderer）。候选：
- **大纲视图**（默认）：提纲树生长 + 右侧 Top 问题/争议
- **逐页 / 卡片视图**：每页一张卡（裁图 + 讲解 + 该页评论 + 该页热力），天然适合纪要与"给没来的人"
- **时间线视图**：主讲沿时间轴一条线，评论/emoji/翻页作为标注挂在对应时刻
- **信息图 / 一页图**：高密度静态总览
- **观点光谱视图**：Polis 式立场地图
- **思维导图 / 知识图谱视图**
- **空间 / 生成式画布**：参考 @poetengineer__ 一脉的手势化、空间化文本呈现（待确认具体形态：手势 reflow / 径向·pinch UI / 粒子星图）

---

## 4. 功能路线图

### 第一期（MVP 骨架）
立住骨架的最小集：
- 主讲流式 ASR → 结构状态（增量 patch）
- **翻页检测 + slide 标题 OCR → 章节骨架**（杠杆高、成本低，优先做）
- 纯文字评论 + 一键 emoji，带时间戳锚定（§2.3）
- 上下文内聚类（§2.4）
- 默认大纲 view + 至少一个备选 view + 切换
- 基础问题浮现（按节点分组）

### 第二期（已点名要做）
- **互动洞察**：困惑热力图（按节点/按页）、观点光谱图、争议点检测、自动投票、共识桥梁
- **Agent 主动性**：主动抛问题、多 persona 边栏、唱反调模式（可开关）、分布式事实核查
- **主讲侧**：一条只有主讲看得到的私有 copilot
- **视觉增强**：图表裁图嵌入画布、指代消解（deixis）、ASR×slide grounding、逐页卡片纪要
- **沉淀**：个人版纪要、演化回放、决议 / Action items 抽取、跨会记忆

### 以后 / 备选（存档备用）
房间即 RAG、实时专家路由、群体预测、迟到追赶（私下补前情）、私人追问、数字分身代言、发言公平性轻推、注意力曲线、节奏雷达、自动高光剪辑、会前期待聚类、承诺跟踪、一鱼多吃导出（纪要/PPT/一页图/推文串/播客脚本）、知识图谱沉淀、现场 vibe-code demo、操纵/带节奏检测、slide-vs-said 差异检测（主讲漏讲/讲错幻灯片上的数据）、白板 / 实物捕获、手势 / 能量分析、丑幻灯片实时重排版、多语言个人视图、文件上传。

---

## 5. 待定 / 下一步
- 采集方式定档：纯摄像头 vs 摄像头 + HDMI/投屏数字源（建议混合）。
- 确认 @poetengineer__ 参考的具体形态。
- 第一期先拆细：**状态模型（含 slide 引用）+ 时间戳锚定 + 上下文聚类 + 翻页检测** 的数据结构与事件 schema。
- 风险位：延迟预算（ASR 流式 + 增量 patch）、噪声（前置过滤）、fact-check 定位（辅助/可溯源，不当裁判）、隐私（匿名 + 主持人否决权 + 摄像头勿扫观众）、视觉鲁棒性（眩光/畸变/遮挡/off-deck）。
