# MindCanvas 使用说明

面向主持人/主讲人/管理员。部署见 [DEPLOY.md](DEPLOY.md)（部署文档）。

---

## 1. 四个入口（每场会议 = 一个房间 `?room=<房间号>`）

| 入口 | 地址 | 登录 | 作用 |
|---|---|---|---|
| 🛠 管理后台 | `/admin` | 需要（管理员） | 建/结束会议、配引擎 Key、管用户、下载音频 |
| 🎙 主讲台 | `/speaker?room=` | 需要（主讲/管理员） | 驱动内容：麦克风 / 上传录音 / 模拟，看实时转写 |
| 👥 观众画布 | `/?room=` | 免登录 | 匿名评论 / emoji / 提问，看实时收敛画布 |
| 📺 大屏展示 | `/screen?room=` | 免登录 | 投影：当前段落核心 + 最新互动聚光灯 |

> 不带 `?room=` 直接访问 `/` 或 `/screen` 会返回 404；`/speaker` 会自动跳到一个新房间。请用主讲人/后台分发的链接进入。

---

## 2. 首次设置（管理员）

1. 打开 `/admin`，用管理员账号登录（默认 `admin`，密码见部署配置）。
2. **配置综合引擎**：填 GLM/Claude 的 Key（可选 Endpoint、模型）→ 保存并校验 → 「✓ 已持久化保存」。不配也能用（启发式降级）。
3. **用户管理**：尽快改掉默认 `admin`/`speaker` 密码；按需新增主讲账号（角色 speaker）或管理员（角色 admin）。

---

## 3. 开一场会（完整流程）

1. **建会议**：管理后台「＋ 新建会议」→ 拿到房间号，自动打开对应主讲台。
2. **开讲**（主讲台三选一）：
   - 🎤 **现场麦克风**（Chrome）：流式 ASR 实时转写、自动分段起小标题。
   - 📁 **上传 mp3 / wav**：录音转写。
   - ▶ **模拟会议 / 真实文稿**：一键演示，无需硬件。
3. **幻灯片（可选）**：在主讲台上传幻灯片图片 → 自动 OCR 出标题/正文（需视觉模型；关了就手填）→ 形成「卡片」。语音旁白会挂在当前幻灯片下。
4. **拉观众**：复制观众链接发出去（同一房间号），听众匿名进入参与。
5. **投大屏**：会场投影打开大屏页。

---

## 4. 实时画布 · 多视图

顶部切换：
- **大纲**：章节 + 要点（讲者内容主线）。
- **卡片**：每页幻灯片一张卡（有幻灯片时）。
- **时间线**：事件按时间轴排列。
- **导图**：根 → 章节 → 要点 思维导图。
- **回放**：演化回放，拖动时间轴看画布如何长出来。
- **纪要**：自动生成的会议纪要（每分钟刷新）。

右栏「参与讨论」：焦点问题（按内容聚类）、观点/反馈、🔎 资料补充。

---

## 5. 观众参与（many-to-one 收敛）

- 观众发评论 / emoji / 提问，自动锚定到「此刻正在讲的内容」，也可点某个节点指定评论对象。
- 系统把几百条声音实时**上下文聚类**成「焦点问题/观点簇」，而不是刷屏。
- 弱网/断线有**断点续传**：评论不丢、不重复，重连自动恢复全量快照。

---

## 6. AI 同事 & 资料补充（增强参与感）

- 在主讲台开启 **🤖 AI 同事**：几位不同身份（产品经理/架构师/怀疑派/新人/数据控/行业观察）不定时就当前内容评论，并会回应真实观众提问。
- **🔎 资料补充**（随 AI 同事开关启用）：
  - `web` 模式：联网检索一条相关事实/数据/定义 + 来源（需 GLM Key + 外网）。
  - `local` 模式：离线检索本地文件（Word/PDF/PPT/txt/md），补充并标注**来源文件 + 页/页码**，只引用库内内容。
    - 放资料：把 `.txt/.md` 丢进 `/data/docs`；Word/PDF/PPT 用 `tools/index_docs.py` 生成索引（见部署文档 §6）。

---

## 7. 纪要 · 导出 · 分享

- **会议纪要**：每分钟自动重算；只保留支撑逻辑链的高信息量内容（过滤寒暄/口头语/重复），并抽出「行动项 / 决议 / 待解答 / 风险」。
- **导出**（主讲台 数据备份/导出）：JSON（完整数据）/ Markdown（纪要）/ TXT（转写）。
- **分享链接**：生成只读的 `/d/<id>` 纪要分享页。

---

## 8. 会议生命周期（结束 = 仅管理后台）

- 主讲台**不能**结束会议（无清空按钮）。
- 管理后台可：**结束**（锁为只读归档）/ **重开** / **删除** / **下载音频** / 查看完整数据。
- 结束后会议信息、转写、音频、纪要完整留在服务器卷，重启/重新部署不丢。

---

## 9. 数据存哪

全部在 `/data` 卷：会议与快照、音频分段、用户、引擎配置、纪要分享快照、本地资料。备份/迁移只需备份这个卷（见部署文档 §7）。

---

## 10. 常见问题

| 问题 | 说明 |
|---|---|
| 打开 `/` 是 404 | 需带 `?room=房间号`；用分发的观众链接 |
| 主讲台进不去 | 需登录（主讲/管理员账号） |
| 没有「卡片」 | 卡片来自幻灯片；纯语音会议无幻灯片，看大纲即可 |
| 资料补充不出现 | 需开 AI 同事；web 模式需外网+Key，local 模式需有本地语料 |
| 看不到 AI 综合 | 未配引擎 Key（走启发式）；在 /admin 配置即可 |
